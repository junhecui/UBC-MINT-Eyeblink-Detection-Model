def apply_lowpass_filter(eeg_data, cutoff_frequency=30.0, sampling_rate = 250):
    """
    Applies a low-pass filter to the EEG data to remove high-frequency noise.
    """

def detect_peaks(eeg_data, delta = 0):
    """
    Detects peaks in the EEG data that could correspond to eye-blink events.
    """